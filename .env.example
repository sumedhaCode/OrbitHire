# Copy to .env.local
# Atlas or local Docker: mongodb://127.0.0.1:27017/orbithire
# If unset, OrbitHire starts an in-memory MongoDB (fine for demos).
MONGODB_URI=
SESSION_SECRET=replace-with-a-long-random-string
